<div class="container">
 <div class="row">
  <h3 class="col-sm-3 bg-secondary text-center mt-5 mb-5">ジャンル編集</h3>
 </div>
</div>

<div class="container">
  <div class="row">
   <table class="col-sm-6">
    <%= form_with model: [:admin, @genre], local:true do |f| %>
     <tr>
      <th>ジャンル名</th>
      <th><%= f.text_field :name %></th>

      <th>
       <div><%= f.submit '変更を保存', class: "btn btn-success btn-block" %></div>
      </th>
     </tr>
    <% end %>
   </table>
  </div>
</div>