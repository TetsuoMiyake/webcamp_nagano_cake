<div class="container">
 <div class="row">
  <h3 class="col-sm-3 bg-secondary text-center mt-5 mb-5">商品一覧</h3>
 </div>
</div>

 <% @items.each do |item| %>
  <%= link_to item_path(item.id) do %>
   <%= attachment_image_tag item, :image, :fill, 100, 100 %>
  <% end %>
  <%= item.name %>
  <%= item.price %>
<% end %>